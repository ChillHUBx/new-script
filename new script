local Library = loadstring(game:HttpGet("https://pastebin.com/raw/vff1bQ9F"))()
local Window = Library.CreateLib("CHill🟧HUB", "Ocean")

-- Tabs

local Tab1 = Window:NewTab("Main/game")
local Tab1Section = Tab1:NewSection("Main/game")

-- Buttons/Windows/Idk

Tab1Section:NewButton("Inf Jumps", "Enables Inf Jumps", function()
    local InfiniteJumpEnabled = true
game:GetService("UserInputService").JumpRequest:connect(function()
	if InfiniteJumpEnabled then
		game:GetService"Players".LocalPlayer.Character:FindFirstChildOfClass'Humanoid':ChangeState("Jumping")
	end
end)
end)

Tab1Section:NewToggle("Fov", "Changes Fov", function(state)
    if state then
        game.Workspace.CurrentCamera.FieldOfView = 120
    else
        game.Workspace.CurrentCamera.FieldOfView = 80
    end
end)

Tab1Section:NewSlider("Speed", "Sussy Speed", 250, 120, function(v)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = v
end)

------end----

local Tab1 = Window:NewTab("setting/RTX♻️")
local Tab1Section = Tab1:NewSection("setting/RTX")

Tab1Section:NewToggle("auto pickup", "Changes Fov", function(state)
    if state then
        auto pickup
_G.on = true --false to turn off and true to turn on
while _G.on do
wait(3) -- you can change the delay here
char = game:GetService('Players').LocalPlayer.Character
game:GetService('Players').LocalPlayer.CharacterAdded:connect(function(body)
char = body end)
for i,v in pairs(workspace:GetDescendants()) do
if v:IsA('Tool') then
char.Humanoid:EquipTool(v) end end
    else
        auto pickup
_G.on = false --false to turn off and true to turn on
while _G.on do
wait(3) -- you can change the delay here
char = game:GetService('Players').LocalPlayer.Character
game:GetService('Players').LocalPlayer.CharacterAdded:connect(function(body)
char = body end)
for i,v in pairs(workspace:GetDescendants()) do
if v:IsA('Tool') then
char.Humanoid:EquipTool(v) end end
end)

Tab1Section:NewToggle("anti cheat", "Changes Fov", function(state)
    if state then
        game.Workspace.CurrentCamera.FieldOfView = 120
    else
        game.Workspace.CurrentCamera.FieldOfView = 80
    end
end)

local Tab1Section = Tab1:NewSection("RTX")

Tab1Section:NewButton("RTX", "♻️♻️", function()

end) 
